# Kevel Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-05-14**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — confirmed base URLs, auth (X-Kevel/X-Adzerk-ApiKey), Decision API (no key required), email-ad cache-busting/`?key`/userKey rules all UNCHANGED. Corrected drift: Management+Reporting rate limit is 50 req/sec combined (was "not documented"); Business/Enterprise plans exist with resource limits (was "no tiers"); UserDB `read`/`interest`/`optout` are GET (pixel) not POST; retargeting endpoint is `/segments` not `/retarget`; Forecast endpoint is `/v1/forecaster` not `/v1/forecast`; added Forget User DELETE endpoint for GDPR erasure and Python/Java/Clojure/iOS/Android SDKs; added Decision/Management SLA figures. Sources: https://dev.kevel.com/reference/getting-started-with-kevel, https://dev.kevel.com/docs/service-level-agreement, https://dev.kevel.com/reference/read-a-users-record, https://dev.kevel.com/reference/add-interest-to-user, https://dev.kevel.com/reference/opt-out-a-user, https://dev.kevel.com/reference/add-segments-to-a-user, https://dev.kevel.com/reference/gdpr-consent, https://dev.kevel.com/reference/forget-user, https://dev.kevel.com/reference/post_forecaster, https://dev.kevel.com/docs/email-ads-overview, https://dev.kevel.com/docs/management-sdks.
