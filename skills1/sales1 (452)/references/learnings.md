# Automate Qwilr with CRM & Tools Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-03-24**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — corrected major request-body drift: page creation uses a single top-level `substitutions` object (NOT per-block `tokens`), `published` (NOT `isPublished`), `templateId` OR `blocks` with block `id`+`foldable`; quote data nests INSIDE the block as `quoteSections`/`quoteSettings` with `lineItems` using `unitPrice`/`quantity`/`optional`/`billingSchedule` (NOT `items`/`fixedCost.amount`/`isOptional`/`billing.*`); variables are strings-only (pre-format dates/numbers); DELETE webhook path is `/webhooks/{subscriptionId}`; docs domain moved developers.qwilr.com → docs.qwilr.com; auth is Bearer with JWT format (unchanged); API access is Enterprise-only + extra fees per Qwilr's own pages. No documented rate limit or webhook HMAC signing found (left as unverified). Sources: https://docs.qwilr.com/api-reference/, https://docs.qwilr.com/docs/getting-started/quick-start/, https://docs.qwilr.com/docs/getting-started/development-cycle/, https://docs.qwilr.com/docs/getting-started/api-concepts/, https://docs.qwilr.com/docs/guides/variables/, https://docs.qwilr.com/docs/guides/quote-data-lifecycle/, https://qwilr.com/api/.
