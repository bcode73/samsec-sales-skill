# Circleback Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-23**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — corrected MCP server URL from app.circleback.ai/api/mcp to circleback.ai/api/mcp; added Claude Code + Codex to compatible MCP clients and noted OAuth uses dynamic client registration; corrected webhook `insights` payload shape (each insight name maps to an array of objects with insight/speaker/timestamp, not a flat string); noted signing secret lives in Settings → Webhook and documented header is X-Signature. Pricing (Individual $20.83/$25, Team $25/$30, Enterprise custom, 7-day trial), webhook HMAC-SHA256 + whsec_ + x-signature, Make trigger-only, and no published rate limits all UNCHANGED. REST API at api.circleback.com still behind a JS-rendered Swagger UI — endpoints/auth remain unverified, left as-is. Sources: https://support.circleback.ai/en/articles/11014015-export-meeting-data-with-webhooks, https://support.circleback.ai/en/articles/13249081-circleback-mcp, https://circleback.ai/blog/how-to-connect-circleback-mcp, https://circleback.ai/pricing, https://support.circleback.ai/en/articles/10756508-integrate-with-apps-using-make.
