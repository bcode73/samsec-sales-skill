# Lobstr.io Platform Help Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-03-29**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — base URL (https://api.lobstr.io/v1), `Authorization: Token` auth, rate limits (squids/runs 120/min, tasks/runtasks 90/min, results 2/sec) + headers + 429 body, webhook events (run.running/paused/done/error), 3-retry/15-min policy, 200-202/30s response, and no-HMAC-signature all UNCHANGED. CHANGED: credit model is now variable per crawler + per enrichment function ("tracked/reported per function", May 2026 update), not a flat 1/result; run status lifecycle adds pending/uploading/aborted; squid config uses `params.max_results` (not `result_limit`); webhook timestamp format is `YYYY/MM/DD HH:MM:SS` UTC (not ISO 8601); CLI installs as `lobstrio` (SDK `lobstrio-sdk`). NEW endpoints added: squid delete/empty/chain, task delete/upload, run abort/stats/credits/download, accounts (list/get/delete/refresh/synchronize), crawlers (list/get/parameters/attributes), /me, /credits/balance, /credits/modules. UNVERIFIED: exact paid-tier prices/names — Capterra + search snippets show Starter ~$10 / Basic ~$30 / Growth ~$50 USD (vs old "from €50/mo"), but the official pricing table is JS-rendered and could not be quoted verbatim; left as a flagged note, not rewritten. Sources: https://docs.lobstr.io/, https://docs.lobstr.io/llms-full.txt, https://docs.lobstr.io/docs/authentication.md, https://docs.lobstr.io/docs/rate-limiting.md, https://docs.lobstr.io/docs/update-squid.md, https://www.lobstr.io/pricing, https://www.capterra.com/p/10018837/lobstr/pricing/
