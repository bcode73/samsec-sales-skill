# Brevo Platform Help Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-03-31**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — fixed two wrong endpoint paths (transactional SMS is `POST /transactionalSMS/send` not `/sms/send`, with stats at `/transactionalSMS/statistics/{aggregatedReport,reports,events}`; transactional WhatsApp is `POST /whatsapp/sendMessage` not `/whatsapp/send`); added concrete per-endpoint rate limits (smtp/email 1,000 RPS / 3.6M RPH, contacts & events 10 RPS / 36K RPH, all-other 100 RPH, with advanced/extended tiers) and confirmed only 3 rate headers exist (no retry-after); added full transactional webhook event list (adds Invalid Email / Deferred / Error / Sent) and marketing webhook event keys (list_addition, contact_updated, contact_deleted, spam, proxy_open, unique_coupon_sent…) plus the no-HMAC-signing fact; corrected pricing — current Marketing Platform tiers are Free / Starter / Standard / Professional / Enterprise, Starter now from $9/mo for 5,000 emails (not $25/20K), Standard from $18/mo is the automation / A-B / landing-page / send-time-optimization tier. Base URL, api-key header, limit/offset pagination, and the 3 x-sib-ratelimit-* headers all UNCHANGED. Could not re-verify each CRM/loyalty/e-commerce endpoint path individually (left as-is). Sources: https://developers.brevo.com/docs/api-limits, https://developers.brevo.com/docs/limit-headers, https://developers.brevo.com/docs/transactional-sms-endpoints, https://developers.brevo.com/reference/send-whatsapp-message, https://developers.brevo.com/docs/how-to-use-webhooks, https://developers.brevo.com/docs/marketing-webhooks, https://www.brevo.com/pricing/, https://help.brevo.com/hc/en-us/articles/208589409-About-Brevo-s-pricing-plans.
