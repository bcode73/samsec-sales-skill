# Hedy AI Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-17**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — minor drift: added EU regional host (eu-api.hedy.bot) for API+MCP, documented now-confirmed endpoint paths (GET /sessions/{id}, GET /highlights, GET /todos, GET /topics/{id}, GET /contexts/{id}), cursor pagination (limit default 50/max 100 + after), Free-tier 1-session-context limit, topic custom-context 20K-char cap, POST body fields (topics: name/description/color/icon; contexts: title/content), X-Hedy-Signature is hex-encoded HMAC-SHA256 with no sha256= prefix, expanded MCP client list (Claude Code/Cursor/Windsurf/Zed/Cline/Perplexity + mcp-remote bridge), and OMI wearable integration. UNCHANGED: Bearer auth, 200 req/min, 5 webhook events + same signature/event headers + retry policy, 18 MCP tools via OAuth 2.1 PKCE, pricing Free/Pro $12.99 ($99.99/yr)/Lifetime $299. CONFLICT left as-is: API help page said "up to 50 webhooks" but dedicated webhooks page says "up to 10" — kept 10 (corroborated by webhooks page). Sources: https://www.hedy.ai/help/hedy-api/, https://www.hedy.ai/help/webhooks/, https://www.hedy.ai/help/connect-hedy-with-ai-assistants-using-mcp/, https://www.hedy.ai/integrations/, https://www.hedy.ai/pricing/
