# Verbit Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-22**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — minor drift fixed: (1) Insights API (Gen V) is hosted on a separate host `https://ai.verbit.co/api/v2/insights` (and `/api/v2/insights/generate`), not the main `api.verbit.co`; (2) added EU (`https://eu.api.verbit.co/api/`) and Sandbox (`https://sandbox-api.verbit.co/api/`) base URLs alongside US; (3) added Verbit's official Streaming Transcription Python SDK `verbit-streaming-sdk` (PyPI / github.com/verbit-ai/verbit-streaming-python-sdk) — the earlier "no official client libraries" note now applies only to the REST APIs. UNCHANGED/confirmed: $29/mo self-service (20 hrs AI), OAuth Bearer via POST users.verbit.co/api/v1/auth (24h expiry), 5 API categories, no public webhooks (poll for completion), rate limits unpublished, Post-Production `/job/*` endpoint style. UNVERIFIED (conflicting/inaccessible across fetches — left unchanged): a possible Post-Production v4 REST surface (`/api/v4/jobs`), a possible `orders.verbit.co` Live Booking host, and `recurrences` vs `recurrence_orders` / `uploads/attachments` vs `presigned_urls` path naming. Sources: https://verbit.readme.io/docs/getting-started, https://verbit.readme.io/docs/insights-gen-ai, https://verbit.readme.io/reference/get_insights_api_v2_insights_get, https://verbit.readme.io/docs/post-production-api, https://verbit.readme.io/docs/live-booking-api, https://github.com/verbit-ai/verbit-streaming-python-sdk, https://sonix.ai/resources/verbit-pricing/.
