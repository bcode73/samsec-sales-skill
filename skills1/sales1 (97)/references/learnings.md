# ChurnZero Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-14**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — REST API v1 (OData) confirmed UNCHANGED on base URL `https://{instance}.us1app.churnzero.net/public/v1/`, HTTP Basic (base64 username:apikey) auth, rate limits soft-max 100 / hard-max 200 per minute (1-100 normal, 101-200 delayed, >200 429), page size 100 via @odata.nextLink. DRIFT/NEW: editions renamed from two-tier Professional/Enterprise to three-tier **Essentials/Growth/Enterprise**, and REST API + custom reporting now unlock at **Growth** (no longer Enterprise-exclusive); pagination also supports `$skiptoken`; `$filter` adds `in` operator and lambda `any/all`; error set adds **409 Conflict** (schema/model changed); REST surface exposes additional entities (UserAccount, SegmentColumnSet, ChurnScoreFactorCalculation, JourneyMilestoneAchievement/Task/Progress/StepProgress, Note, NoteType, Message, Meeting, MeetingType). Push/Analytics API UNCHANGED (base `https://analytics.churnzero.net/i`, appKey query param, GET-only, setAttribute/trackEvent/incrementAttribute/deleteContact/deleteAccount). Renewal & Forecast Hub exact Growth-vs-Enterprise gating not officially published — left UNVERIFIED. Sources: https://app.churnzero.net/developers, https://churnzero.com/features/rest-api/, https://www.stitchflow.com/user-management/churnzero/api, https://hightouch.com/docs/destinations/churnzero, https://www.vendr.com/marketplace/churnzero, https://bigryg.churnzero.com/editions/
