# Salesloft Platform Help Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-03-24**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — minor drift: corrected webhook event-type count (38→37), added webhook signature verification (x-salesloft-signature = HMAC-SHA1 hex digest keyed with callback_token, plus x-salesloft-event header) and retry policy (3 retries 15s apart) which were previously undocumented, and fixed SKILL.md rate-limit line (team-level, not per-user). Verified UNCHANGED: base URL api.salesloft.com/v2, OAuth auth-code + client-credentials flows (7200s tokens, refresh revocation), API key format ak_+64hex with partner OAuth-only restriction, 600 cost/min team-level rate limit + high-page cost table + x-ratelimit headers. Full 37-general-scope list could not be confirmed (scopes page renders only the "commonly used" subset) — left intact, unverified. Sources: https://developers.salesloft.com/docs/platform/api-basics/rate-limits/, https://developers.salesloft.com/docs/platform/api-basics/oauth-authentication/, https://developers.salesloft.com/docs/platform/api-basics/client-creds/, https://developers.salesloft.com/docs/platform/api-basics/api-key-authentication/, https://developers.salesloft.com/docs/platform/webhooks/event-types/, https://developers.salesloft.com/docs/platform/webhooks/delivery-headers/, https://developers.salesloft.com/docs/platform/webhooks/introduction/, https://developers.salesloft.com/docs/api/.
