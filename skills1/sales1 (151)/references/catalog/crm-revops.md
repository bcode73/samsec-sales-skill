# CRM, RevOps & Integration


## /sales-attio

Attio platform help — AI-native CRM with custom objects, relationship database, deal pipelines, email sequences, automations, and built-in enrichment for 80,000+ startups. Covers custom data model design (objects, attributes, relationships), pipeline configuration, workflow automations, email sequences, AI agents (Ask Attio), reporting, API integration (REST, OAuth 2.0, 100+ endpoints, webhooks, MCP server), and pricing tiers (Free/Plus/Pro/Enterprise). Use when Attio data model doesn't fit your workflow, deal pipeline stages need restructuring, automations aren't firing correctly, API or webhook integration isn't working, or not sure if Attio is the right CRM for your team. Do NOT use for general CRM data hygiene strategy (use /sales-data-hygiene), outbound sequences across platforms (use /sales-cadence), contact enrichment strategy (use /sales-enrich), or tool integration patterns (use /sales-integration).


## /sales-aviso

Aviso AI platform help — end-to-end AI revenue operating system with 98%+ forecast accuracy, conversation intelligence, relationship intelligence, MIKI AI Chief of Staff, pipeline inspection, deal acceleration, and 200+ integrations. Use when setting up Aviso forecasting for complex revenue models (splits, overlays, consumption-based), confused about which Aviso forecast type to trust (AI vs sales vs adjusted), conversation intelligence not capturing buyer emotions or sentiment correctly, MIKI AI agent workflows not triggering or producing wrong outputs, Aviso dashboard customizations reverting unexpectedly, choosing between Aviso and Clari or Gong for revenue intelligence, CRM writeback not updating Salesforce or HubSpot fields, or evaluating Aviso pricing for your team size. Do NOT use for picking between note-takers generally (use /sales-note-taker) or building a coaching program (use /sales-coaching).


## /sales-boomi

Boomi platform help — enterprise iPaaS, 1000+ connectors, API Management, Data Hub MDM, Flow low-code builder, Event Streams, B2B/EDI, AgentStudio AI agents, MCP support. Use when Boomi integration keeps failing or data isn't syncing, connector won't authenticate to SAP or Salesforce, per-connection pricing is spiraling and you need to optimize, debugging a Boomi process is painful with vague error messages, evaluating Boomi vs MuleSoft vs Workato, or setting up API management and governance. Do NOT use for simple Zapier/Make automations (use /sales-integration) or MuleSoft-specific questions (use /sales-mulesoft).


## /sales-celigo

Celigo platform help — mid-market iPaaS, 1000+ connectors, AI-powered error management, endpoint+flow pricing, NetSuite depth, API Management, B2B/EDI, Embedded iPaaS, Celigo Ora AI agent. Use when integration errors are vague and you can't figure out what's failing, field mapping between apps is confusing or broken, evaluating Celigo vs Workato vs Boomi vs MuleSoft for your team, NetSuite integration keeps throwing invalid reference errors, flows stop processing data and you don't know why, or pricing tiers don't make sense and you need to optimize endpoints/flows. Do NOT use for simple Zapier/Make automations (use /sales-integration) or Workato/MuleSoft/Boomi-specific questions (use their platform skills).


## /sales-crm-selection

CRM comparison, selection, and migration strategy for startups — evaluates Attio, HubSpot, Salesforce, Folk, Pipedrive, Close, Zoho, and spreadsheets. Use when choosing a first CRM, deciding if you've outgrown spreadsheets, comparing CRMs for a small team, planning a CRM migration, figuring out when to upgrade from free tier, or evaluating whether you need Salesforce yet. Do NOT use for Attio-specific config or API (use /sales-attio), CRM data cleanup (use /sales-data-hygiene), or outbound sequences (use /sales-cadence).


## /sales-customersai

Customers.ai platform help — X-Ray identity resolution pixel, Alfred AI intent scoring, Inboxer deliverability suite, ad retargeting audience recovery, Consumer Data Enrichment API. Use when anonymous Shopify visitors aren't converting to email, Klaviyo is only identifying 15-20% of traffic, visitor ID contacts are hurting deliverability, Klaviyo costs too much from bloated profiles, you're comparing Customers.ai vs Retention.com vs RB2B, or ad retargeting audiences shrank after cookie deprecation. Do NOT use for B2B visitor identification (use /sales-rb2b), email marketing strategy (use /sales-email-marketing), or email list growth strategy across tools (use /sales-audience-growth).


## /sales-engagebay

EngageBay platform help — affordable **all-in-one CRM + marketing + sales + service suite** for startups and SMBs (engagebay.com): three modules — **Marketing Bay** (email campaigns, automations/sequences, landing pages, web forms, SMS, broadcasts), **Sales & CRM Bay** (contacts, companies, deals on tracks/milestones, tasks, lead scoring, appointment scheduling, call logs, products), and **Service Bay** (helpdesk tickets, live chat, AI chatbot) — on one shared contact database. Pitched as "HubSpot power at a fraction of the cost"; a budget **HubSpot / Keap / Ontraport / Agile CRM / Zoho** alternative with a genuine free tier. Developer surface: **REST API** (base `https://app.engagebay.com/`, auth = **raw REST API Key in the `Authorization` header — no `Bearer`, no OAuth**, from Account → Admin Settings → API; **responses default to XML — send `Accept: application/json` for JSON**; **cursor pagination** via `page_size` max 100 + `cursor` returned on the LAST record; resources `subscribers` (contacts), companies, deals, tracks, events, tasks, notes, forms, sequences, lists, owners, custom fields (every write needs `field_type`), tickets, tags, products, broadcast). **Listing contacts is a POST** (`POST dev/api/panel/subscribers`). **Webhooks are OUTBOUND ONLY** — EngageBay does **not accept incoming webhooks** (write into EngageBay via the REST API), and outbound payloads are **unsigned (no HMAC)** — events `contact.created/updated/deleted`, `company.*`, `deal.created/updated/deleted`, `email.open`, link clicked, replied; payload `{event, entity:{id, properties}}`; destination URL supports mustache personalization `{{entity.id}}`. **Deals attach to a case-sensitive `track_id` + `milestoneLabelName`** — a wrong-case milestone silently hides the deal from the pipeline view. **Monthly API-call quota is plan-gated** (~750k/mo on Pro, far lower below) → `429`; errors 400/401/404/429. SDKs: Java/.NET/JavaScript/PHP. Pricing (best-effort, All-in-One Suite, per user): Free $0 (250 contacts) → Basic ~$14.99 (500) → Growth ~$64.99 (5,000) → Pro ~$119.99 (50,000); Marketing Bay and CRM Bay sold standalone for less. 30% recurring affiliate program. Use when an EngageBay API call returns XML instead of JSON, you can't push data INTO EngageBay because it has no incoming webhooks, a deal created via the API doesn't appear in the pipeline (track/milestone case mismatch), you hit a 429 from the plan-gated API quota, per-user/per-contact pricing keeps climbing, automation-node or contact limits are too low on your tier, syncing EngageBay contacts/deals to a warehouse or another CRM, or weighing EngageBay as a cheaper HubSpot/Keap alternative. EngageBay, engagebay.com, all-in-one CRM, Marketing Bay Sales CRM Bay Service Bay, app.engagebay.com REST API, Authorization API key header no Bearer, Accept application/json XML default, dev/api/panel/subscribers, cursor page_size pagination, track_id milestoneLabelName case-sensitive, outbound-only webhooks no incoming, unsigned webhook no HMAC, contact.created deal.updated email.open, 750000 API calls per month quota 429, per-contact per-user pricing, free tier 250 contacts, helpdesk tickets live chat, HubSpot/Keap/Ontraport/Agile CRM/Zoho alternative. Do NOT use for comparing CRMs across vendors (use /sales-crm-selection) or email-marketing strategy across tools (use /sales-email-marketing).


## /sales-salesmate

Salesmate platform help — **AI-powered sales CRM** for SMB and mid-market teams (salesmate.io, by Rapidops): contacts/companies, **deal pipelines**, activities/tasks, **built-in calling + SMS** (power dialer, voicemail drop), **email sequences**, marketing automation (campaigns, lead scoring, A/B tests), **Smart Flow** workflow automation, ticketing/team inbox, quotes/products, meeting scheduler, web forms, and the **Sandy AI** copilot + AI Auto-pilot agents with call transcription. An affordable Pipedrive/Close/Freshsales/HubSpot alternative whose differentiator is native voice/SMS plus AI across the funnel. Developer surface: **RESTful v4 API** (base `https://{link_name}.salesmate.io/apis/{module}/v4/...`; auth = **API Key in headers** `accessToken` (your Access Key from My Account → Access Key) + `x-linkname` (your account host e.g. `demo.salesmate.io`) + `Content-Type: application/json` — **not** a Bearer token; resources contact/company/deal/activity/notes/users/custom-modules; module IDs Contact 1, Task 2, Email 3, Deal 4, Company 5, Product 6, Team Inbox 7; **search/list is a POST** to `/apis/{module}/v4/search?rows=250&from=0` with a `displayingFields`+`filterQuery`+`sort` body; **offset pagination** via `rows`+`from` with `totalRows`/`totalPages` in `Data`; response envelope `{Status, Data}` or `{Status:"failure", Error:{Code,Name,Message}}`; errors 400/401/403/404; **rate limit 1500 API calls/hour per link**; **v1/v3 deprecated May 1 2023 — use v4**). **No inbound API webhooks** — push events OUT via a Smart Flow "API Call/Webhook" action or Zapier/Make/Pipedream "Watch …" triggers; write IN via REST. Pricing (best-effort, per user/mo, 15-day free trial): Basic $23 (CRM only, no sequences/AI), Pro $39 (sequences, **Sandy AI**, quotes, 10K Smart Flow credits), Business $63 (power dialer, custom modules, SLAs, 15K credits), Enterprise custom (onboarding from $1,999); calling/SMS numbers are a paid add-on from ~$1.1/mo + usage; 24/7 support Enterprise-only. 700+ integrations; GitHub org github.com/salesmate; 20% recurring affiliate / Solution Partner up to 30%. Use when a Salesmate API call returns 401 because the `accessToken`/`x-linkname` headers are wrong, you're sending a Bearer token and it fails, syncing Salesmate contacts/deals to a warehouse or another CRM, hitting the 1500-calls/hour per-link limit, search/pagination via `rows`+`from` confuses you, Sandy AI or sequences are locked because you're on the Basic plan, RingCentral call logging keeps needing re-authorization, Smart Flow credits run out mid-automation, or weighing Salesmate vs Pipedrive/Close/Freshsales. Salesmate, salesmate.io, Rapidops, AI sales CRM, Sandy AI copilot, built-in calling SMS power dialer voicemail drop, sequences, Smart Flow workflow credits, accessToken x-linkname header API key auth, {link_name}.salesmate.io/apis v4, apis contact company deal activity v4, search rows from offset pagination, totalRows totalPages, 1500 API calls per hour per link, v1 v3 deprecated, module ids Contact 1 Task 2 Deal 4 Company 5, no inbound webhooks API Call Webhook Smart Flow, Status Data envelope ValidationError, RingCentral re-authorization, Basic Pro Business Enterprise plan gates, Pipedrive/Close/Freshsales/HubSpot alternative. Do NOT use for comparing CRMs across vendors (use /sales-crm-selection) or outbound cadence strategy across tools (use /sales-cadence).


## /sales-hubspot

HubSpot platform help — Smart CRM with Marketing Hub, Sales Hub, Service Hub, Content Hub, Data Hub, Commerce Hub, and Breeze AI. Covers deal pipeline setup, email sequences, workflow automations, lead scoring, marketing email campaigns, landing pages, forms, live chat, chatbots, reporting dashboards, and API integration (REST, OAuth 2.0, date-versioned endpoints, webhooks, 5 SDKs). Use when HubSpot workflows aren't firing, email sequences not sending, lead scoring feels off, reports don't show what you need, marketing contacts are too expensive, trying to connect HubSpot to other tools, or not sure which Hub or tier you need. Do NOT use for CRM comparison and selection (use /sales-crm-selection), outbound cadence design across platforms (use /sales-cadence), or CRM data cleanup strategy (use /sales-data-hygiene).


## /sales-informatica

Informatica IDMC platform help — enterprise iPaaS + data management cloud, Cloud Data Integration, Cloud Application Integration, API Center, Data Catalog, Data Quality, MDM, B2B Gateway, AI Agent Engineering, IPU consumption pricing, REST API v2/v3. Use when Informatica IDMC mapping or task keeps failing, CDI job is slow and IPU consumption is spiraling, Secure Agent is offline or can't reach source systems, REST V2 connector won't authenticate to a REST API, evaluating Informatica vs Boomi vs MuleSoft vs Talend, migrating off PowerCenter to IDMC, or configuring MDM golden records and match rules. Do NOT use for simple Zapier/Make automations (use /sales-integration) or Boomi-specific questions (use /sales-boomi).


## /sales-insightly

Insightly platform help — **unified CRM + project management + marketing + service** platform for SMB/mid-market (insightly.com), sold as four separately-licensed modules (CRM, Marketing, Service, **AppConnect** built-in no-code iPaaS, 2,000+ connectors). Differentiator: a **won Opportunity converts into a delivery Project** on the same account graph — strong for agencies, consultancies, and professional-services teams that sell *and* deliver. Core objects: Contacts, **Organisations** (British spelling), Leads, Opportunities, Projects, Tasks, Events, Notes, Pipelines/PipelineStages, Tags, Products/Pricebooks/Quotes (Enterprise), Users, CustomFields. Developer surface: **REST API v3.1** — base `https://api.{pod}.insightly.com/v3.1` where **pod** (`na1`/`eu1`/…) comes from User Settings; auth = **HTTP Basic with the API key as the Base64-encoded username and a blank password** (un-encoded key → `401`; the in-browser Help sandbox auto-encodes, masking the bug); pagination `top` (default 100, max 500) + `skip` + `count_total=true` → `X-Total-Count` header; **plan-gated daily request quota** (Free/legacy ~1,000 → Plus 40,000 → Professional 60,000 → Enterprise 100,000) plus a **10 req/sec** ceiling → `429`; headers `X-RateLimit-Limit`/`X-RateLimit-Remaining`; **ETag + `If-Match`** optimistic concurrency on `PUT` (`412` on stale); custom fields are a **`CUSTOMFIELDS` array** of `{FIELD_NAME, FIELD_VALUE}` pairs (discover via `GET /CustomFields/{objectName}`); sync on `DATE_UPDATED_UTC`. **Webhooks have no subscription API** — they're a **Webhook action inside a Workflow Automation rule** (workflow automation is **Professional+**), fire on record add/update, POST the record's field IDs+values, support HTTP Basic on the callback, and **retry only twice** then drop the event → pair with a reconciliation pull. **No email sequencing on any plan**; built-in **reporting is limited** (multi-object reports hard, project budgets don't roll up to opportunities); **custom objects, products/pricebooks/quotes, and sandboxes are Enterprise-only**. AppConnect, Zapier, Make, Pipedream wrap the API; native Google Workspace/M365/QuickBooks/Xero/Mailchimp. GitHub org github.com/Insightly. Pricing (best-effort, no permanent free tier, 14-day trial): CRM Plus $29 / Professional $49 / Enterprise $99 per user/mo; Marketing $99–$999/account/mo; Service $29–$99/user/mo; AppConnect $249–$1,899/mo by task volume; All-in-One bundle from $349/$899/$2,599/mo. Use when an Insightly API call returns `401` because the key wasn't Base64-encoded, finding your pod/region for the base URL, hitting the daily request cap (`429`), paginating with `top`/`skip`/`count_total`, syncing contacts/organisations/opportunities into a warehouse or another CRM, a webhook only fires from a workflow rule or stops after a failure, working around limited reporting or no email sequencing, custom objects locked to Enterprise, the opportunity→project handoff, or choosing Plus vs Professional vs Enterprise (and CRM vs Marketing vs Service vs AppConnect). Insightly, insightly.com, CRM plus project management, opportunity to project, Organisations British spelling, REST API v3.1, api.{pod}.insightly.com, na1 eu1 pod region base URL, Base64 API key Basic auth blank password 401, top skip count_total X-Total-Count pagination, daily request quota 1000 40000 60000 100000, 10 requests per second 429, X-RateLimit-Remaining, ETag If-Match 412 concurrency, CUSTOMFIELDS array FIELD_NAME FIELD_VALUE, DATE_UPDATED_UTC incremental sync, workflow automation webhook action retries twice, AppConnect iPaaS 2000 connectors, no email sequencing, custom objects products pricebooks quotes Enterprise only, Pipedrive/Zoho/Copper/HubSpot alternative, agency consultancy professional services CRM. Do NOT use for comparing CRMs across vendors (use /sales-crm-selection), CRM data cleanup/dedupe (use /sales-data-hygiene), outbound cadence design (use /sales-cadence), or generic iPaaS wiring beyond AppConnect (use /sales-integration).


## /sales-integration

Connects sales tools with webhooks, Zapier/Make, native integrations, and custom API pipelines — CRM sync, event triggers, data mapping, and error handling. Use when CRM data isn't syncing from Mailshake/Apollo/Salesloft, webhook events aren't firing or arriving malformed, Zapier/Make automations are failing or skipping records, data is duplicating or missing between tools, or an integration that was working suddenly breaks. Do NOT use for Qwilr-specific automations (use /sales-qwilr-automation), general CRM platform config (use /sales-apollo or /sales-salesloft), or marketing automation flows (use /email-sequence).


## /sales-jitterbit

Jitterbit platform help — AI-infused low-code iPaaS (Harmony), 400+ connectors, API Manager, App Builder, EDI, Design Studio, Integration Studio, Agent Management. Use when Jitterbit process keeps failing with vague errors or no stack trace, Salesforce or NetSuite connector fails silently on individual record updates, debugging a Harmony integration is painful with limited logging, per-connection pricing is making costs unpredictable as you add endpoints, evaluating Jitterbit vs Workato or Boomi or MuleSoft, setting up hybrid agents behind a firewall, or looking for API Manager and API gateway configuration. Do NOT use for simple Zapier/Make automations (use /sales-integration) or Boomi-specific questions (use /sales-boomi).


## /sales-bitrix24

Bitrix24 platform help — **free-forever all-in-one business suite** (bitrix24.com) with a full **CRM** (leads, deals, **pipelines/funnels**, contacts, companies, **Smart Process Automation** custom objects) plus tasks/projects, **omnichannel contact center** (web forms, live chat, WhatsApp/Instagram, telephony, email), sites/store builder, marketing (email/SMS/ads), and collaboration (chat/video/docs). **Cloud or self-hosted on-prem.** Its hook is **unlimited users on the free plan** → a popular free **Keap/Ontraport/Zoho/HubSpot-free alternative** for solo founders + SMBs (also scales to enterprise); 740+ app **Market**; **MCP server** for AI tools. Developer surface: **method-based REST API (RPC, not resource paths)** — URL `https://{portal}.bitrix24.com/rest/{user_id}/{webhook_code}/{method}.json` for **inbound webhooks** (static URL + secret code, **no expiry**, permission-scoped; created at Applications → Developer resources → Incoming webhook) or **OAuth 2.0** for multi-tenant/Market apps & local applications. Core CRM methods follow `*.add/.update/.get/.list/.delete/.fields`: **`crm.lead.*`**, **`crm.deal.*`** (fields TITLE, **`CATEGORY_ID`** = pipeline, **`STAGE_ID`** = crm_status, OPPORTUNITY, CONTACT_IDS, ASSIGNED_BY_ID; add-response `{"result":<id>,"time":{…}}`), `crm.contact.*`, `crm.company.*`, `crm.status.*` (stages), `crm.dealcategory.*` (funnels), and **`crm.item.*`** (SPA). **Outbound webhooks** fire on events (`ONCRMDEALADD/UPDATE`, `ONCRMLEADADD`) POSTing `{event, data:{FIELDS:{ID}}}` + an **application token** (verify it; payload carries only the ID → re-fetch via `*.get`). **`batch`** runs up to 50 commands/request (chain with `$result[...]`); rate-limited (~2 req/sec + per-method operating-time) so batch + back off. Some methods (telephony, certain app-only ops) are unavailable to webhooks. Pricing (best-effort): Free (unlimited users, caps) → Basic/Standard/Professional/Enterprise (fixed user cap per tier) → On-Premise license. Use when adding/updating leads or deals via the API or a webhook, reacting to CRM events, mapping pipelines/stages (CATEGORY_ID/STAGE_ID), batching to dodge rate limits, choosing inbound webhook vs OAuth, adopting the free plan as a Keap/Ontraport alternative, or deciding cloud vs on-prem. Bitrix24, bitrix24.com, free forever CRM unlimited users, all-in-one business suite, CRM tasks contact center sites collaboration, method-based REST API RPC, rest user_id webhook_code method, crm.deal.add crm.lead.add, CATEGORY_ID pipeline STAGE_ID stage, crm.item Smart Process Automation, inbound outbound webhook, ONCRMDEALUPDATE application token, OAuth 2.0 local application, batch 50 commands $result, rate limit 2 per second, 740 app Market, MCP server, self-hosted on-premise, Keap/Ontraport/Zoho/HubSpot-free alternative. Do NOT use for CRM selection across vendors (use /sales-crm-selection), CRM/RevOps strategy across tools (use /sales-crm), or generic iPaaS wiring (use /sales-integration).


## /sales-keap

Keap platform help — all-in-one small-business CRM + marketing automation (formerly Infusionsoft; owned by Thryv). Covers the drag-and-drop Campaign Builder / "When-Then" automations, contacts + tags, email + SMS marketing, sales pipeline/opportunities, invoicing + quotes + recurring subscriptions + payments, appointments, webforms, lead scoring, built-in referral/affiliate tracking, and REST API integration (v1/v2, base `https://api.infusionsoft.com/crm/rest/v2/`, OAuth 2.0 + Personal Access Tokens + Service Account Keys, cursor/offset pagination, OAuth ~1,500 req/min + 25 req/sec spike cap, REST Hooks webhooks for contact/opportunity/order/payment/tag events with HMAC-SHA256 signing, batching, and 4 retries). Use when Keap automations or campaigns aren't triggering (goal never met), the Campaign Builder feels too complex, per-contact pricing keeps climbing, Keap throttled or blocked your account over spam complaints (it counts unsubscribe-with-feedback as a complaint), deciding between Max Classic and the newer Keap Pro/Max/Ultimate interface, building a Keap API or webhook integration, syncing Keap contacts/tags/orders to another CRM or data warehouse, or can't pull campaign sequences via the API. Keywords: Keap, Infusionsoft, Max Classic, Keap Ultimate, Campaign Builder, When-Then automation, small business CRM, marketing automation, per-contact pricing, REST Hooks, infusionsoft.com API, HubSpot alternative, GoHighLevel alternative service business. Do NOT use for comparing Keap against other CRMs (use /sales-crm-selection), multi-step funnel strategy across tools (use /sales-funnel), email-marketing strategy (use /sales-email-marketing), or SMS-marketing compliance (use /sales-sms-marketing).


## /sales-maximizer

Maximizer (maximizer.com) platform help — long-standing **Canadian CRM**, cloud "CRM Live" or **on-premise**, for SMB sales teams and distinctively **financial-services / wealth-advisory** firms (Financial Advisor edition: households, investment/insurance views, family connections, compliance). Developer surface: two APIs. The **Octopus API** (a.k.a. **Maximizer.Web.Data**, base `https://api.maximizer.com/octopus`) is a **POST-RPC** JSON service — every operation is a POST to a verb path: `/Authenticate`, `/Read`, `/Create`, `/Update`, `/Delete`, `/Validate`, `/BinaryUpload`, `/BinaryDownload/{key}`, `/WorkflowStart`, `/TokenValid`/`/VerifyToken`. A `/Read` body uses **`Scope.Fields`** (fields to return), **`Criteria.SearchQuery`** (operators `$EQ`/`$NE`/`$GT`/`$GE`/`$LT`/`$LE`/`$IN`/`$NIN`/`$ALL`/`$LIKE`/`$NLIKE`/`$PHRASE`/`$RANGE`/`$NRANGE`, logical `$AND`/`$OR`, date helpers `$NOW()`/`$TODAY()`/`$THISWEEK()`/`$THISMONTH()`/`$THISYEAR()`/`$OFFSET()`), **`GroupBy`** (`$COUNT`/`$MIN`/`$MAX`/`$AVG`/`$SUM`), **`OrderBy`** + `Top` for pagination, and a mandatory **`Configuration.Drivers`** searcher (e.g. `"IAbEntrySearcher":"Maximizer.Model.Access.Sql.AbEntrySearcher"`, `IOpportunitySearcher`, `ILeadSearcher`, `ICaseSearcher`) — a wrong/missing driver returns an **empty result, not an error** (the #1 "my query returns nothing" cause). Objects: **AbEntry** (Company/Contact/Individual), Opportunity, Lead, Case, Note, Activity/Appointment, Campaign, User/SalesTeam/Territory, InteractionLog. Records are addressed by an opaque **base64 composite `Key`** (read first, reuse the Key on update/delete — never hand-build it); responses carry a numeric **`Code`** (0 = success) even on HTTP 200. **Custom fields (UDFs)** use **`Udf/$TYPEID`** notation. Auth: **Personal Access Token (PAT, Bearer)**, `/Authenticate` (cloud needs `Database`/`UID`/`Password`/`VendorId`/`AppKey`; on-premise omits VendorId/AppKey), or **OAuth2**. **Rate limits are per-edition** — Core ~30 calls/10s, Business/Financial ~90/30s, Enterprise custom → `429` (and `408` on slow single calls, `401` on bad token). Separate **Webhooks API** (base `https://api.maximizer.com/webhooks`): register a **target** (`POST /v1/targets`, enable/disable) then a **subscription** (`POST /v1/subscriptions` with `Entity`/`Op`/`TargetId`/`Filter`); delivered payload `{Id, Timestamp, ProcessedTimestamp, WorkspaceId, Entity, Data:{Op, Original, New, Version}}`; your endpoint must return **200 within 2 seconds**, Maximizer **retries twice then discards** (3 attempts), and there is **no documented HMAC signature** (secure via IP allowlist / secret URL). Legacy **Ferret API** is being superseded by Octopus; Power BI connector for reporting; Zapier; OpenAPI + Postman collections at `github.com/maximizercrm`; `developer.maximizer.com/llms.txt`. Pricing (best-effort, annual; monthly ~10–20% higher; **no free tier**, 30-day trial): Base/For Sales ~$65, Sales Leader ~$79, Financial Advisor ~$79–100 per user/mo; on-premise quote. Use when a Maximizer Octopus `/Read` returns nothing because `Configuration.Drivers` names the wrong searcher, building an AbEntry/Opportunity/Lead export to a warehouse or another CRM, a webhook silently stops after the 2-second/3-attempt delivery limit, hitting the per-edition rate limit, wiring base64 Keys or `Udf/$TYPEID` custom fields, linking Outlook email to records without endless clicks, choosing Base vs Sales Leader vs Financial Advisor (or cloud vs on-premise). Keywords: Maximizer, maximizer.com, Octopus API, Maximizer.Web.Data, api.maximizer.com/octopus, POST-RPC /Read /Create /Update /Delete, Scope Criteria Configuration Drivers, IAbEntrySearcher IOpportunitySearcher, AbEntry Company Contact Individual, base64 composite Key, Code 0 success, Udf $TYPEID UDF custom field, $EQ $LIKE $RANGE $OFFSET operators, GroupBy $COUNT $SUM, OrderBy Top pagination, Personal Access Token PAT Bearer OAuth2 Authenticate VendorId AppKey, per-edition rate limit 30 90 calls 429 408, Webhooks API v1 targets subscriptions, 2-second 200 ACK 3-attempt retry discard, no HMAC signature, Ferret API legacy, Financial Advisor edition households family connections wealth advisory insurance, cloud CRM Live on-premise, Power BI connector, github.com/maximizercrm, developer.maximizer.com llms.txt, financial services CRM, Pipedrive Salesforce alternative regulated advisory team. Do NOT use for comparing Maximizer against other CRMs (use /sales-crm-selection) or generic iPaaS/Zapier wiring (use /sales-integration).


## /sales-mulesoft

MuleSoft platform help — Anypoint Platform, API-led connectivity, Design Center, Anypoint Studio, Code Builder, Exchange, Runtime Manager, API Manager, Flex Gateway, Composer, RPA, IDP, DataWeave, CloudHub, 450+ connectors. Use when Anypoint Studio crashes or gives misleading errors, DataWeave transformation isn't working, CloudHub deployment fails or runs out of CPU credits, API policies aren't enforcing correctly, connectors won't authenticate to SAP or Salesforce, vCore pricing is spiraling and you need to optimize, or MuleSoft implementation is stalling. Do NOT use for general CRM platform config (use /sales-salesforce) or simple Zapier/Make integrations (use /sales-integration).


## /sales-ontraport

Ontraport platform help — all-in-one CRM + marketing automation + payments + dynamic CMS on one unified database for established small/mid-market businesses (ontraport.com): contacts, visual pipelines, lead scoring/routing, Campaign Builder visual automation, email + SMS, order forms/subscriptions with upsells and decline recovery, membership/course/portal pages, and a shared inbox. A Keap/GoHighLevel/ActiveCampaign competitor. Developer surface: object-based REST API (base `https://api.ontraport.com/1`, auth via `Api-Key` + `Api-Appid` request headers from Administration → Integrations; everything is an object addressed by a numeric `objectID` — Contact = `0`, discover the rest via `GET /1/objects/meta`; endpoints `/1/objects`, `/1/object`, `/1/objects/saveorupdate` upsert, `/1/objects/tag`; offset pagination via `range` (max 50) + `start` + `listFields`/`sort`/`condition`; rolling rate limit of **180 requests/minute** returning `429`; interactive Swagger doc at `api.ontraport.com/doc` + live tester `api.ontraport.com/live`) plus webhooks subscribed via the API (events: object created, form submitted, tag added, tag removed, product purchased, transaction added; logs under Administration → Integrations → Webhook Logs, max 10,000). Pricing (best-effort, per-contact): Basic ~$79/1,000 contacts, Plus ~$147, Pro ~$297 (adds reporting + partner programs), Enterprise ~$497; +$46/user; overages stack. Use when building an Ontraport API integration to sync contacts/transactions into a CRM or warehouse, figuring out the objectID model or why there's no `/contacts` endpoint, hitting the 180-requests-per-minute rate limit, paginating objects with `range`/`start`, subscribing to a new-sale or tag webhook, the steep learning curve has you stuck, per-contact overage charges are escalating the bill, the clunky email editor is fighting you, or choosing Basic vs Plus vs Pro vs Enterprise. Ontraport, ontraport.com, all-in-one CRM, marketing automation, Campaign Builder, object API, objectID, api.ontraport.com/1, Api-Key Api-Appid headers, /objects/meta, saveorupdate, range start pagination, 180 requests per minute, transaction_added webhook, per-contact pricing, Keap/GoHighLevel/ActiveCampaign/Infusionsoft alternative. Do NOT use for choosing a CRM across vendors (use /sales-crm-selection), email-marketing or deliverability strategy (use /sales-email-marketing or /sales-deliverability), membership/course strategy (use /sales-membership), or checkout optimization across platforms (use /sales-checkout).


## /sales-pipedrive

Pipedrive platform help — **pipeline-first sales CRM** for SMB sales teams (pipedrive.com): visual drag-and-drop **deal** pipelines + stages, **leads** inbox (LeadBooster), **persons**/**organizations**, **activities** (calls/tasks/meetings + calendar sync), workflow automation, email sync, reporting/forecasting, AI assistant, and a 500+ app marketplace. A clean, easy-to-adopt HubSpot/Zoho/Close/Salesforce alternative for outbound/deal-centric teams of ~3–50; weaker on native reporting depth and pushes capability into paid add-ons (LeadBooster, Web Visitors, Campaigns, Smart Docs, Projects). Developer surface: a mature **REST API with two live versions** — **v2** (use this; base `https://{company-domain}.pipedrive.com/api/v2`, auth via **`x-api-token` header**, **cursor pagination** `limit`+`cursor`, **`PATCH`** for updates, ~50% cheaper token cost) and legacy **v1** (`/api/v1`, `?api_token=` query param, offset `start`+`limit`, **full shutdown 2026-07-31**); **Leads/Notes/Webhooks/Files/Filters/Goals/Projects are still v1-only**. Auth is **API token** (per-user, Settings → Personal → API) for single accounts or **OAuth 2.0** for Marketplace apps (call the `api_domain` returned at token exchange). Response envelope `{success, data, additional_data}`. **Custom fields are referenced by random 40-char hash keys** (map via `GET /api/v1/dealFields`/`personFields`/`organizationFields`). **Webhooks** (`action.object` filters — added/updated/deleted/merged × deal/person/org/activity/note/pipeline/product/stage/user; v2 payload `{meta, data:{current, previous}}`; retries 3/30/150s; **30-min ban after 10 first-attempt fails**; **auto-deleted after 3 days of no 2xx**; HTTP Basic Auth on receiver) are **free — they don't consume API tokens**. **Token-based rate limits**: daily budget = **30,000 × plan multiplier (Lite 1× / Growth 2× / Premium 5× / Ultimate 7×) × seats**; `429` when exhausted (per-day, not per-second); per-user burst limit on a rolling 2s window; use `updated_since` for incremental sync. OpenAPI 3 specs (v1+v2), Postman collection, free sandbox accounts; no official MCP server. GitHub org github.com/pipedrive. **API + webhooks are on every plan — not plan-gated; only the token budget scales.** Pricing (best-effort, renamed late-2025): Lite ~$14, Growth ~$39, Premium ~$49–59, Ultimate ~$79 per seat/mo (annual); was Essential/Advanced/Professional/Power/Enterprise. Use when building a Pipedrive API or webhook integration, deciding between v1 and v2 or migrating before the July 2026 v1 shutdown, hitting the daily token `429`, custom fields showing as 40-char hashes, webhooks duplicating/missing or auto-deleting, syncing deals/persons into a warehouse or Slack, choosing OAuth vs API token, or picking a plan/understanding the token multiplier. Pipedrive, pipedrive.com, sales CRM, pipeline, deals, leads, persons organizations activities, x-api-token, api v2 cursor pagination, api_token v1 shutdown 2026-07-31, PATCH, 40-char custom field hash, dealFields, token-based rate limit 429, plan multiplier, free webhooks, action.object, current previous payload, OAuth api_domain, OpenAPI sandbox, LeadBooster, HubSpot/Zoho/Close/Salesforce alternative. Do NOT use for CRM selection/comparison or migration across vendors (use /sales-crm-selection), CRM data cleanup/dedupe (use /sales-data-hygiene), outbound cadence design (use /sales-cadence), or generic iPaaS wiring (use /sales-integration).


## /sales-salesforce

Salesforce platform help — Sales Cloud, Service Cloud, Marketing Cloud, Commerce Cloud, Data Cloud, Agentforce AI, Platform (Lightning/Apex/Flow), AppExchange, and REST API (v66.0, OAuth 2.0, SOQL, Bulk API 2.0). Use when Salesforce workflows or Flows aren't firing, reports don't show the right numbers, lead assignment rules aren't working, Apex triggers are failing, AppExchange app won't install, need help with SOQL queries, implementation is stalling, data migration to or from Salesforce, or not sure which Salesforce edition you need. Do NOT use for CRM comparison and selection (use /sales-crm-selection) or CRM data cleanup strategy (use /sales-data-hygiene).


## /sales-scratchpad

Scratchpad platform help — Salesforce-native AI workspace with call recording, AI field updates, pipeline sheets, Hygiene Monitor, and Deal Agent. Use when reps waste time manually updating Salesforce after calls, pipeline views in Salesforce are clunky and reps avoid them, CRM data is stale because reps don't log activities, evaluating Scratchpad vs Weflow vs Gong vs Clari for Salesforce teams, or Scratchpad AI credits running out too fast. Do NOT use for general CRM data cleanup strategy (use /sales-data-hygiene) or choosing a CRM (use /sales-crm-selection).


## /sales-snaplogic

SnapLogic platform help — enterprise iPaaS, 1000+ Snap connectors, AgentCreator AI agents, SnapGPT pipeline copilot, API Management 3.0, ETL/ELT/reverse ETL, visual pipeline designer. Use when pipeline debugging is painful and errors are hard to trace, evaluating SnapLogic vs Workato vs MuleSoft vs Boomi vs Celigo, building AI agents with AgentCreator or Prompt Composer, managing APIs with Policy Studio and Developer Hub, premium Snap Pack costs are unclear, or connecting enterprise apps with visual pipelines. Do NOT use for simple Zapier/Make automations (use /sales-integration) or Workato-specific questions (use /sales-workato).


## /sales-tableau

Tableau platform help — Tableau Desktop, Tableau Cloud, Tableau Server, Tableau Prep, Tableau Pulse, Embedding API, REST API (v3.28, PAT/JWT auth, 300+ endpoints), MCP server, and Tableau+. Use when dashboards are slow with large datasets, LOD expressions or calculated fields aren't working, licensing costs are confusing or spiraling, Tableau won't connect to Salesforce or your data warehouse, embedded analytics aren't rendering, Tableau Prep flows keep failing, or you need help choosing Creator vs Explorer vs Viewer licenses. Do NOT use for general CRM config (use /sales-salesforce) or sales forecasting methodology (use /sales-forecast).


## /sales-tibco

TIBCO Cloud Integration platform help — enterprise iPaaS combining TIBCO Scribe (Connect), Flogo (Develop), BusinessWorks (Integrate), Cloud Live Apps (Automate), Cloud API Management, app-instance subscription tiers, REST API v1, hybrid on-prem + cloud deployments. Use when a TIBCO Flogo REST trigger is failing or mapping a field wrong, TIBCO Scribe replication is pulling all history with no filter, a Scribe job hit Execution timeout or duplicate-key errors, BusinessWorks hybrid agent won't connect to TIBCO Cloud, hitting the 5 or 15 app instance limit on Basic/Premium plan, evaluating TIBCO vs Boomi vs MuleSoft vs Informatica, or migrating from on-prem TIBCO BusinessWorks to TCI. Do NOT use for simple Zapier/Make automations (use /sales-integration) or Informatica-specific questions (use /sales-informatica).


## /sales-tray

Tray.ai platform help — enterprise iPaaS with 700+ connectors, Intelligent iPaaS, Enterprise Core governance, Merlin Agent Builder for AI agents, Tray Embedded for SaaS vendors, GraphQL API, Connector Development Kit. Use when Tray bill keeps climbing and task consumption is unpredictable, workflows fail with unclear errors and debugging feels opaque, evaluating Tray vs Workato vs MuleSoft vs Boomi, embedding integrations into a SaaS product via Tray Embedded, building Merlin AI agents, or configuring the GraphQL Embedded API and solution instances. Do NOT use for simple Zapier/Make automations (use /sales-integration), Workato-specific questions (use /sales-workato), or MuleSoft-specific questions (use /sales-mulesoft).


## /sales-weflow

Weflow platform help — Salesforce-native Revenue AI with activity capture, conversation intelligence, and deal forecasting. Use when Salesforce data is stale because reps don't update it, emails and meetings aren't logging to Salesforce automatically, need AI call recording and transcription inside Salesforce, pipeline forecasts are inaccurate because CRM data is garbage, or comparing Weflow vs Gong vs Clari vs Scratchpad for Salesforce teams. Do NOT use for general CRM data cleanup strategy (use /sales-data-hygiene) or choosing a CRM (use /sales-crm-selection).


## /sales-workato

Workato platform help — enterprise iPaaS, 1200+ connectors, recipe-based automation, API Management, MCP Gateway, Agent Studio (Genies AI agents), Data Tables, Event Streams, B2B/EDI, Embedded iPaaS, IDP. Use when recipe costs keep spiraling and you need to optimize task consumption, recipe errors are unclear and debugging is painful, evaluating Workato vs MuleSoft vs Boomi vs Celigo, setting up API management or MCP gateway, building Genies AI agents in Agent Studio, or connecting enterprise apps without coding. Do NOT use for simple Zapier/Make automations (use /sales-integration) or MuleSoft-specific questions (use /sales-mulesoft).


## /sales-gohighlevel

GoHighLevel (HighLevel) platform help — all-in-one "business operating system" for agencies/SMBs/freelancers (gohighlevel.com): CRM + sales pipelines, funnels/websites/landing pages, email + SMS + WhatsApp + social DMs, workflows/automations, calendars, reputation/review management, payments/invoicing, memberships/courses, conversation & voice AI, affiliate manager, and white-label SaaS resale. Agency model = Agency → Sub-Accounts (Locations) → Snapshots (reusable setup templates) → SaaS mode (resell under your brand with Stripe rebilling). Developer surface: v2 REST API (base `https://services.leadconnectorhq.com`, OAuth 2.0 for Marketplace apps or Private Integration Tokens for internal tools, **required `Version: 2021-07-28` header**, most resources scoped to a `locationId`, rate limit 100 requests/10 seconds burst per resource/location + a daily cap, `meta`/`startAfterId` pagination) covering contacts, opportunities, conversations/messages, calendars, and payments; 50+ outbound webhooks signed with Ed25519 `X-GHL-Signature` (legacy RSA `X-WH-Signature` deprecated 2026-07-01), retried up to 12× with exponential backoff (dedupe on `webhookId`). Pricing: Starter $97/mo (3 sub-accounts, no white-label), Unlimited $297/mo (unlimited sub-accounts + white-label), SaaS Pro $497/mo (SaaS resale + rebilling); SMS/email/phone/AI usage billed separately via LeadConnector. Use when building a GoHighLevel v2 API or webhook integration, a Private Integration Token's scopes are too narrow (403s), hitting the 100-req/10s rate limit, syncing contacts/opportunities/conversations into or out of GHL, verifying Ed25519 webhook signatures or migrating off the deprecated RSA signature, deduping retried webhooks, setting up sub-accounts and snapshots for clients, white-labeling or configuring SaaS-mode rebilling, automating pipelines/workflows, or choosing Starter vs Unlimited vs SaaS Pro. GoHighLevel, HighLevel, GHL, go high level, all-in-one CRM, agency CRM, white-label CRM, SaaS mode, rebilling, sub-account, location, snapshot, LeadConnector, services.leadconnectorhq.com, Version 2021-07-28 header, Private Integration Token, X-GHL-Signature, HubSpot alternative agency, Keap/Kartra/Systeme.io/Ontraport alternative. Do NOT use for choosing a CRM across vendors (use /sales-crm-selection), multi-step funnel strategy across tools (use /sales-funnel), email-marketing strategy (use /sales-email-marketing), or SMS-marketing strategy/compliance (use /sales-sms-marketing).
