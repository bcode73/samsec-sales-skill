# Awario Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-05-04**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — Get Alerts endpoint corrected to `GET /alerts/list` (was guessed `/alerts`) with new response shape (`alert_id`/`alert_type` "mentions"|"leads"/`alert_name`/`project_id`/`project_name` + `limits` array); mention object fields aligned to docs (`done`, `starred`, `author.audience/description`, `entities` not `entity_info`; no top-level `country`); insights `time_series` types expanded to the full 9 (was only mentions/sentiment/reach); pricing currency corrected EUR→USD with identical numbers ($29/$89/$249 annual, $49/$149/$399 monthly), pricing page lists "API access" Included on all plans while API auth docs still require Enterprise/custom; auth (access_token query param, Owner/Admin), rate limits (100 req/sec IP, 300 req/min token), all error codes, no Twitter/X via API, and no-webhook all UNCHANGED. Sources: https://awario.com/pricing/, https://awario.com/help/api/api-overview/, https://awario.com/help/api/api-authentication/, https://awario.com/help/api/get-alerts-list/, https://awario.com/help/api/get-mentions/, https://awario.com/help/api/get-insights/, https://awario.com/help/api/rate-limits/, https://awario.com/help/api/error-codes/
