# G2 Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-13**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — current docs front the v2 API (base `data.g2.com/api/v2/`, test endpoint `GET /api/v2/products`); OAuth scopes updated to the `openid profile products.read products.reviews.read` family (older `buyer_intent.read`/`screenshots.read` scopes deprecated/changed); added concrete G2 MCP Server details (OAuth, 4 datasets, Claude Connector setup), the 20+-field Buyer Intent data dictionary (company-level only, no contacts), the ~1,000 calls/month free tier, and clarified that HubSpot/Salesforce "webhooks" are lead-form delivery webhooks distinct from API RESThooks; rate limit (100 req/s + 60s block), 1-year token expiry, portal URL, and the Capterra/Software Advice/GetApp acquisition all confirmed UNCHANGED. Pricing left unverified (g2.com/products/g2/pricing is login/403-gated). Sources: https://documentation.g2.com/docs/developer-portal, https://documentation.g2.com/docs/g2-api-information, https://documentation.g2.com/docs/g2-mcp-server, https://documentation.g2.com/docs/buyer-intent-data-reference, https://documentation.g2.com/docs/hubspot-webhook, https://documentation.g2.com/docs/salesforce-crm-webhook, https://partner.g2.com/developer.
