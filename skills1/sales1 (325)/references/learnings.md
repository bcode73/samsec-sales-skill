# Mailgun Platform Help Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-03-29**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — pricing changed (NEW Basic plan from $15/mo for 10,000 emails/mo no daily limit; Foundation/Scale now advertised "free for 1 month"; official pricing page lists one dedicated IP included on Foundation 50K+ and Scale, additional IPs $59/IP/mo; Optimize Pilot $49/Starter $99 quotas confirmed), NEW v4 webhook endpoints `POST/PUT/DELETE /v4/domains/{domain}/webhooks` (up to 3 URLs per event) plus account-level `/v1/webhooks`, NEW Metrics usage endpoint `POST /v1/analytics/usage/metrics`, NEW `engagement` field on v4 validation response; UNCHANGED — base URLs api.mailgun.net / api.eu.mailgun.net, HTTP Basic Auth api:KEY, Domain Sending Keys (/messages + /messages.mime only), `POST /v3/{domain}/messages` fields + 1,000-recipient batch cap, HMAC-SHA256 webhook signing over timestamp+token, validation result/risk values, official SDKs (Ruby/Python/PHP/Node.js mailgun.js/Java/Go/C#), docs at documentation.mailgun.com. Sources: https://www.mailgun.com/pricing/, https://documentation.mailgun.com/docs/mailgun/api-reference/mg-auth, https://documentation.mailgun.com/docs/mailgun/api-reference/send/mailgun/messages, https://documentation.mailgun.com/docs/mailgun/api-reference/send/mailgun/webhooks, https://documentation.mailgun.com/docs/mailgun/api-reference/send/mailgun/metrics, https://documentation.mailgun.com/docs/validate/api-overview, https://documentation.mailgun.com/docs/mailgun/sdk/introduction.
