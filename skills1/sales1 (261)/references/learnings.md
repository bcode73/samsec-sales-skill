# Jamie Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-17**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — added base URL `https://beta-api.meetjamie.ai`; corrected endpoints to RPC-style `meetings.list/.get/.delete/.search`, `tasks.list`, `tags.list` (GET reads, POST delete, search personal-only); documented `?input={"json":...}` request format, `result.data.json` envelope, `nextCursor` pagination, and rate-limit/error headers; added webhook limits (3/user, 5/workspace), exact signature format `t=,v0=` with 5-min replay window and `{timestamp}.{raw_body}` signing, exact retry schedule (immediate/10s/60s/10m/1h + dead-letter), headers (`jamie-event`, `jamie-delivery`, `Jamie-Webhooks/1.0`), and full payload shape; noted native Jamie Agent/MCP feature (Settings → Agent) alongside the community MCP server; added Dynamics 365 Sales + Pipedrive to CRM list. UNVERIFIED: webhook plan gate is contradictory in official docs (Getting Started says "Plus or higher", Overview lists "Pro, Team, Enterprise") — left existing Plus+ claim unchanged; annual pricing not re-confirmable on current pages. Sources: https://docs.meetjamie.ai/llms-full.txt, https://www.meetjamie.ai/blog/llm-info, https://docs.meetjamie.ai/pages/webhooks/getting_started.
