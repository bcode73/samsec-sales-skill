# Klaviyo Platform Help Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-03-29**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — corrected webhook drift (signature header is `Klaviyo-Signature` not `Klaviyo-Webhook-Signature`, plus `Klaviyo-Timestamp` + `Klaviyo-Webhook-Id` headers; topic IDs use `event:klaviyo.opened_email`/`clicked_email`/`bounced_email`/`sent_sms`/`subscribed_to_email_marketing` format not `email.opened`/`profile.subscribed`; max 10 webhooks/account; secret_key ≥16 chars; added Get Webhook Topic singular endpoint). Added concrete rate-limit tiers (XS 1/s·15/m, S 3/s·60/m, M 10/s·150/m, L 75/s·700/m, XL 350/s·3500/m, assigned per-endpoint, 429 returns Retry-After). Clarified OAuth uses authorization-code + PKCE (S256). Auth (Klaviyo-API-Key header + revision), base URLs, JSON:API format, pagination, and pricing (Free $0/250 profiles/500 emails/150 SMS, Email from $20, Email+SMS from $35) confirmed UNCHANGED. Sources: https://developers.klaviyo.com/en/docs/working_with_system_webhooks, https://developers.klaviyo.com/en/reference/webhooks_api_overview, https://developers.klaviyo.com/en/reference/get_webhook_topics, https://developers.klaviyo.com/en/docs/rate_limits_and_error_handling, https://developers.klaviyo.com/en/docs/authenticate_, https://developers.klaviyo.com/en/docs/set_up_oauth, https://www.klaviyo.com/pricing.
