# Reletter Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-05-11**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — REST layer drift corrected: auth is the `x-reletter-api-key` header (NOT `Authorization: Bearer`), base URL `https://api.reletter.com` with paths under `/api/...` (publications/issues/contacts/charts/search/accounts/payments), contacts endpoint is `/api/contacts/?publication_id=<slug>`, filters DSL is `name:operator:value` comma-separated (e.g. `subscribers:gte:5000,active:is:true`), pagination fields are `page/per_page/count/more` (default per_page 25, max 100), rate limits add a 2,000 req/team/hour burst plus monthly tiers $59/5K up to $429/50K, and an agent-native HTTP 402/MPP purchase flow exists (`POST /api/payments/buy/`). Pricing tiers (Light $49 / Standard $99 / Business $199, API +$59/5K) UNCHANGED; 7M+ newsletters, 5 platforms, SDK 1.0.2 (2026-05-09, Py3.8+, MIT), CLI exit codes, error classes, and no-webhooks all confirmed. Added GitHub repos (getreletter/reletter-python, getreletter/reletter-mcp) and llms.txt/llms-full.txt references. Sources: https://reletter.com/developers, https://reletter.com/llms-full.txt, https://reletter.com/pricing, https://pypi.org/pypi/reletter/json, https://github.com/getreletter/reletter-python.
