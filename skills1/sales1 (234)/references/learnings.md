# HypeAuditor Platform Help Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-12**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — report endpoint paths were wrong: live docs use `auditor.youtube`/`auditor.tiktok`/`auditor.twitter`/`auditor.twitch`/`auditor.snapchat` with a `channel` param (not the old `auditor.youtubeReport`/`auditor.tiktokReport`/etc with `username`/`channel_id`); added Snapchat report + Instagram `auditor.reportByUserId`/TikTok `auditor.tiktokByUserId` ID variants, the `type` param (report vs advanced_report, default advanced_report), `auditor.planInfo` quota endpoint, Media Plan API v1.0, and corrected Market Analysis to `POST /api/marketanalysis/instagramCompetitorAnalysis` (Instagram-only, different base path). Corrected Discovery filter names (`subscribers_count`/`er`/`account_geo`, not `followers_from`/`engagement_rate_from`) and Creator Profile Info date params to ISO datetime. Auth (X-Auth-Id/X-Auth-Token), <100 req/min rate limit, v1-deprecated-Aug-2024, no webhooks all UNCHANGED. Pricing left as-is: Basic $299/mo confirmed by HypeAuditor's own blog; PRO/Enterprise still "tailored"/custom per official source (third-party $499 figure NOT used). Sources: https://hypeauditor.readme.io/reference/basic, https://hypeauditor.readme.io/llms.txt, https://hypeauditor.readme.io/reference/report_instagram, https://hypeauditor.readme.io/reference/report_tiktok, https://hypeauditor.readme.io/reference/report_youtube, https://hypeauditor.readme.io/reference/report_twitter, https://hypeauditor.readme.io/reference/report_twitch, https://hypeauditor.readme.io/reference/report_snapchat, https://hypeauditor.readme.io/reference/general_discovery, https://hypeauditor.readme.io/reference/creator-profile-info.md, https://hypeauditor.readme.io/reference/market_analysis_api.md, https://hypeauditor.readme.io/reference/reports-and-requests-left.md, https://blog.hypeauditor.com/how-much-does-hypeauditor-cost-/
