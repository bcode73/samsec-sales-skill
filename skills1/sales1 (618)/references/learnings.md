# Tray.ai Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-16**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — corrected Embedded API token model (Solution Instance lifecycle mutations `createSolutionInstance`/`updateSolutionInstance`/`upgradeSolutionInstance`/`removeSolutionInstance` all require the **User token**, not Master; `get-solution-instances` takes `ownerId` and accepts user OR master token); fixed `updateSolutionInstance` params to `solutionInstanceId/instanceName/enabled` (config/auth values live in `upgradeSolutionInstance`); user-token mutation is `authorize` (→`accessToken`); added end-user mutations `createExternalUser`/`updateExternalUser`/`removeExternalUser`/`generateAuthorizationCode`/`createUserAuthentication`; added 2-second create→enable→webhook delay gotcha; replaced conflicting pricing with official tray.ai/pricing tier structure (Pro 3 ws/7-day, Team 20 ws/30-day + add-ons, Enterprise unlimited/180-day + SSO/HIPAA/regional/log-streaming); added Agent Gateway for MCP (MCP server governance, A2A). UNCHANGED: base URLs (tray.io/eu1/ap1 /graphql), bearer auth, server-side-only/CORS, rate limits (30 rps/1800 rpm/50 burst/1000 Call-Connector concurrency), 700+ connectors, falafel CDK, GitHub repos. Sources: https://tray.ai/pricing, https://tray.ai/documentation/developer/openapi/embeddedapi/tag/solutionInstances/, https://tray.ai/documentation/developer/getting-started/prerequisites/master-and-user-tokens, https://tray.ai/documentation/developer/embedded-apis/users, https://tray.ai/documentation/developer/developer-portal/getting-started/troubleshooting/rate-limiting-tray/, https://tray.ai/platform/agent-gateway/, https://tray.ai/platform/merlin-agent-builder/.
