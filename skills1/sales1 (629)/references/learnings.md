# Typeform Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-14**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — API surface stable (base URLs, 2 req/s rate limit, Responses API params incl. page_size max 1000/default 25, webhook retry/auto-disable policy, Python SDK still archived, @typeform/api-client and @typeform/embed still active all UNCHANGED). Drift fixed: pricing updated (Basic $28→$29, Plus $56→$59, Business $91→$99, Talent →$169, plan "Growth Pro" renamed to "Growth Flow" ~$349/mo); Create API form `type` enum gained `classification_branching` and `score_branching`; documented webhook signature header `Typeform-Signature` = `sha256={base64 HMAC-SHA256 of raw body}` (only sent when `secret` is set). Exact Talent/Growth Flow dollar figures vary between the pricing page's monthly/annual toggle states — treat as approximate. Sources: https://www.typeform.com/pricing/, https://www.typeform.com/developers/get-started/, https://www.typeform.com/developers/responses/reference/retrieve-responses/, https://www.typeform.com/developers/webhooks/secure-your-webhooks/, https://www.typeform.com/developers/create/reference/create-form/, https://github.com/Typeform.
