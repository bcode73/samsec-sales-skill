# Treasure Data Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-14**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — minor drift. Developer docs migrated from docs.treasuredata.com to docs.treasure.ai (301 redirect); LLM API is now a first-class documented API with regional base URLs (llm-api.treasuredata.com / llm-api.eu01.treasuredata.com / llm-api.treasuredata.co.jp / llm-api.ap02.treasuredata.com) and the same `Authorization: TD1 …` auth; regional endpoint tables now also list an Ingestion API (same host as the JS SDK). Confirmed UNCHANGED: TD API base https://api.treasuredata.com/v3, TD1 auth scheme, core endpoints (GET /database/list, POST /job/issue/presto/{db}, GET /job/result/{job_id}, GET /result/list), Postback case-sensitivity, Personalization API US/EU/JP only (no Korea), and pricing ($100K–$750K/yr, two tiers Intelligent CDP + AI Marketing Cloud, "No Compute" Profiles+Behaviors model). Request-rate limits still undocumented, but TD publishes hard upper limits (query length, 90-day job lifetime, records.in 1–500 events/5MiB, 4096 cols/table, 30 workflow tasks, 5 Profiles API tokens/call) now captured in the API reference. Sources: https://docs.treasure.ai/apis/td-api, https://docs.treasure.ai/apis/endpoints/endpoints, https://docs.treasure.ai/apis, https://docs.treasure.ai/apis/llm-api, https://docs.treasure.ai/apis/p13n-api, https://docs.treasure.ai/requirements-and-limitations/list-of-upper-limits-max-on-treasure-data, https://www.treasuredata.com/blog/ai-native-pricing.
