# Postmark Platform Help Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-03-29**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — minor drift: free tier renamed "Developer"→"Free" (now allows up to 10 custom domains, hard 100-email cap, no overages); added the webhook security model (NO HMAC signing — Basic HTTP auth + IP allowlisting + HTTPS; 403 stops retries; specific retry schedules); added broadcast SMTP host smtp-broadcasts.postmarkapp.com, port 25, and SMTP Tokens (Access/Secret key) auth; fixed suppression-delete method in platform-guide (POST not DELETE); confirmed dedicated IP gated to Pro tier + 300K/mo, inbound email gated to Pro+, message-stream cap 10/server, batch 500/50MB, recipient cap 50 each To/Cc/Bcc — all unchanged. Core endpoints, base URL, auth headers, and webhook payloads all current. Sources: https://postmarkapp.com/pricing, https://postmarkapp.com/blog/new-we-made-pro-and-platform-tier-features-accessible-to-lower-volume-email-plans, https://postmarkapp.com/developer/api/email-api, https://postmarkapp.com/developer/webhooks/webhooks-overview, https://postmarkapp.com/developer/api/suppressions-api, https://postmarkapp.com/developer/user-guide/send-email-with-smtp, https://postmarkapp.com/developer/user-guide/inbound/inbound-domain-forwarding.
