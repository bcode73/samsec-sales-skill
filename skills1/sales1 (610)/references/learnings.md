# Tomba Platform Help Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-03-28**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — fixed wrong endpoint paths (Email Verifier is `GET /v1/email-verifier/:email`, Email Sources `GET /v1/email-sources/:email`, Phone is `GET /v1/phone/:email` keyed on EMAIL not domain); added documented per-endpoint rate limits (Domain Search/Email Finder 15/sec·900/min, Enrichment 5/sec, Verifier/Author 2.5/sec, LinkedIn 1.67/sec·100/min) + `X-RateLimit-Limit/Remaining/Reset` headers (skill previously said limits were undocumented); added now-documented endpoints Account `GET /v1/me`, Usage `/v1/usage`, Logs `/v1/logs`, API Keys `/v1/keys` CRUD, Leads `/v1/leads` CRUD, Lead Lists `/v1/leads_lists` CRUD, Lead Attributes `/v1/attributes`; added domain-search `type` (personal/generic) param; pricing has moved to a credit-based slider ($8.90/1,000 credits, 10,000=$89/mo, verification bundled free, 12-mo validity, 3 months free annual, Free=25 credits/mo) — legacy named $39/89/189/389 ladder left in place but flagged UNVERIFIED. UNVERIFIED/left untouched: webhooks/`webhook_url` (no webhook docs found on docs.tomba.io), `enrich_mobile`/`country` domain-search params, standalone `/v1/email-format` endpoint vs attribute (ambiguous). Sources: https://docs.tomba.io/authentication, https://docs.tomba.io/rate-limits, https://docs.tomba.io/features, https://docs.tomba.io/api/finder, https://docs.tomba.io/api/leads, https://docs.tomba.io/api/keys, https://docs.tomba.io/usage-and-quotas, https://github.com/tomba-io/docs, https://tomba.io/pricing, https://docs.tomba.io/faqs.
