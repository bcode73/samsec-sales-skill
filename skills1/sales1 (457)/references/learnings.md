# Read.ai Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-17**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — REST API moved out of "username/password OAuth" to **Bearer-token auth** (`Authorization: Bearer …`, OAuth 2.1 + dynamic client registration); concrete versioned endpoints now exist (`GET /v1/meetings`, `GET /v1/meetings/{meeting_id}` with `expand=summary,metrics`, Retrieve a Live Meeting); added **100 req/min/user rate limit → 429** and **cursor-based pagination**; meeting states Ended (`end_time_ms`) vs Live-Enabled (`live_enabled:true`); webhook signing confirmed HMAC **SHA-256** over raw body in `X-Read-Signature`; pricing unchanged ($0/$19.75/$29.75/$39.75 mo) but Enterprise+ minimum is now **5+ licenses** (was 10+). Help Center (support.read.ai) still 403s on direct fetch — endpoint/field specifics drawn from current official-page search snippets + the live /plans-pricing page. Sources: https://support.read.ai/hc/en-us/articles/49381161088659-API-Reference, https://support.read.ai/hc/en-us/articles/49380809380371-API-Keys-Authentication, https://support.read.ai/hc/en-us/articles/49379985941523-Read-AI-API-and-MCP-Overview, https://support.read.ai/hc/en-us/articles/16352415827219-Getting-Started-with-Webhooks, https://www.read.ai/plans-pricing, https://www.read.ai/post/read-ai-mcp-your-meetings-just-became-your-most-powerful-dev-tool
