# Fathom Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->

**2026-06-13**: API re-verified against live official docs — added a NEW heavy-request rate limit (30 calls/60s, dropping to 5/60s in high-activity periods, applies to the recordings transcript/summary endpoints and any GET /meetings with include_summary/include_transcript — the binding limit for transcript backfills, distinct from the global 60/min); added the NEW GET /meeting_types endpoint (list-meeting-types page, new since the 2026-04 capture); added the NEW include_highlights query param + highlights[] response field (type/summary/text/start_time/end_time) on GET /meetings; documented the meeting_type filter param; corrected AI Scorecards / coaching metrics to Business-only (the live pricing page lists them under Business and explicitly NOT under Team — the skill previously said "Team and Business"). Verified UNCHANGED: base URL https://api.fathom.ai/external/v1, X-Api-Key auth + Bearer, webhook HMAC (whsec_ prefix, webhook-id/webhook-timestamp/webhook-signature headers, ${id}.${timestamp}.${body} signed content, ~5-min tolerance), create-webhook requires ≥1 include_* and triggered_for enum (my_recordings/shared_external_recordings/my_shared_with_team_recordings/shared_team_recordings), OAuth scope public_api + token endpoint /oauth2/token, SDK v0.0.30 (fathom-typescript / fathom-python), pricing Free $0 / Premium $20-$16 / Team $19-$15 (2 min) / Business $34-$25 (2 min), CRM cap max 3 users/domain. Sources: https://developers.fathom.ai/api-overview.md, https://developers.fathom.ai/api-reference/meetings/list-meeting-types.md, https://developers.fathom.ai/api-reference/meetings/list-meetings.md, https://developers.fathom.ai/api-reference/webhooks/create-a-webhook.md, https://developers.fathom.ai/webhooks.md, https://developers.fathom.ai/sdks/oauth.md, https://developers.fathom.ai/sdks/breaking-changes/sdk-maturity.md, https://fathom.ai/pricing.
