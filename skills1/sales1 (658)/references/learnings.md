# Wave Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-17**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — daily rate limit corrected 1,000→10,000 req/day per token (60/min unchanged); added NEW surface from the OpenAPI spec: action-items endpoint (GET /v1/sessions/{id}/action-items + PATCH write-back with If-Match), folders (GET/POST /v1/folders, POST/DELETE /v1/sessions/{id}/folders/{folderId}, scope folders:write), pull-based event feed (GET /v1/events, POST /v1/events/ack, scope events:read), webhook event session.action_items.updated, and the @waveai/cli CLI; base URL, Bearer auth, wave_api_ token format, 1-year token expiry, mcp.wave.co MCP server, HMAC-SHA256 webhook headers/formula, max-5 webhooks, bulk-50, 1-hour media URLs, 76-language transcription, and Free/Pro $11.67/Teams $7.50-user-min-5 pricing all UNCHANGED. The wave-developer-api blog page is stale (still shows 1,000/day, omits new endpoints) — api.wave.co landing + /v1/openapi.json are authoritative. Sources: https://api.wave.co/, https://api.wave.co/v1/openapi.json, https://wave.co/agents, https://wave.co/pricing, https://wave.co/updates/wave-developer-api.
