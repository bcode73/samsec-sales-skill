# SparkLoop Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-13**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — core API unchanged (base URL `api.sparkloop.app/v2`, X-Api-Key auth, 120 req/60s, page/per_page max 200, all subscriber/campaign/upscribe/offers endpoints, all 7 webhook events with SparkLoop-Token header + 200-blank response all confirmed). NEW endpoints added to the reference: Partner Programs (list/get/update/referrals), Partner Network directory (programs + join, publications + recommend), Publications CRUD, expanded Upscribe recommendations (generate/list + pin/unpin/pause/unpause), and Offers conversion confirmation (POST .../conversion). Also documented v1 API still live at /v1. PRICING DRIFT: Referral Programs are now closed to NEW SparkLoop users (legacy only; Kit users route via Kit's Creator Network) — was a $2K/yr add-on; Partner Program is now two tiers (Optimized $100/mo min, Custom $2K/mo min) — was a flat $2K/mo minimum; brand Offers pay $2–$200+ per confirmed conversion. NOTE: small-model page reads truncated GitBook content and falsely reported only 4 webhook events / no SparkLoop-Token header — the GitBook `.md?ask=` interface confirmed the full 7 events + header, so those claims were left intact. Sources: https://docs.sparkloop.app/, https://docs.sparkloop.app/api-v2/endpoints, https://docs.sparkloop.app/api-v2/endpoints/subscribers.md, https://docs.sparkloop.app/api-v2/endpoints/webhooks.md, https://docs.sparkloop.app/llms-full.txt, https://docs.sparkloop.app/api-1, https://help.sparkloop.app/en/articles/9184574-the-ultimate-guide-to-sparkloop-features-pricing
