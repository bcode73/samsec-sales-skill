# Calendly Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-26**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs — SIGNIFICANT drift: Calendly added a Scheduling API (POST /invitees, "Create Event Invitee") that CREATES bookings, plus write endpoints POST/PATCH /event_types (1:1 only), POST /one_off_event_types, GET/PATCH /event_type_availability_schedules, GET /location, and Enterprise-only activity-log + delete-invitee/delete-event endpoints; the old "API is read-only / cannot create bookings or event types or set availability" framing is now obsolete and was corrected across SKILL.md, platform-guide.md, calendly-api-reference.md. Pricing structure (Free 1 event type / Standard $10-12 / Teams $16-20 / Enterprise $15k+) and webhook events (invitee.created, invitee.canceled, routing_form_submission.created — the last org-scope only) UNCHANGED. Rate limits left as-is but flagged approximate (live rate-limit page is JS-rendered, third-party numbers conflict). Sources: https://developer.calendly.com/schedule-events-with-ai-agents, https://developer.calendly.com/frequently-asked-questions, https://calendly.com/blog/api-dev-portal, https://community.calendly.com/api-webhook-help-61/scheduling-api-now-available-4825, https://community.calendly.com/api-webhook-help-61/new-event-type-management-apis-4109, https://calendly.com/pricing.
