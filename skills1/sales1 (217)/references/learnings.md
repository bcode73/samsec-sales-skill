# Gumroad Learnings

Accumulated tips, gotchas, and corrections discovered during use. Claude reads this at the start of each invocation and appends new learnings as they're discovered. Once significant learnings have accumulated, use `/sales-request-skill` to share them back to the community. Shared and declined entries are marked so they won't be re-prompted.

<!-- Add entries below in format: **YYYY-MM-DD**: Learning description -->
**2026-04-13**: Research baseline (from git history) — platform docs were captured on/around this date and the API surface, pricing, and webhooks have NOT been re-verified against live docs since. Re-verify specifics before relying on them.
**2026-06-13**: API re-verified against live official docs (gumroad.com/api Mintlify reference + antiwork/gumroad routes.rb & licenses_controller.rb). Drift fixed: resource_subscriptions create, products enable/disable, and sales mark_as_shipped/refund are all PUT (not POST); licenses/verify `increment_uses_count` defaults to TRUE (skill said false); missing product_id on post-2023 products returns HTTP 500. NEW endpoints added: Payouts (GET /payouts, /payouts/:id, /payouts/upcoming + `view_payouts` scope), account-wide Refund Policy (GET/PUT /refund_policy), Categories (GET /categories), Thumbnail (POST/DELETE), files/abort + direct_uploads, sales/summary + sales/exports. Pricing unchanged: 10% + $0.50 direct, 30% Discover, $0 monthly. Webhook still x-www-form-urlencoded with x-gumroad-signature HMAC-SHA256, 8 resource_name events unchanged. Rate limit still 429-only (no published numbers). Sources: https://gumroad.com/api, https://github.com/antiwork/gumroad/blob/main/config/routes.rb, https://github.com/antiwork/gumroad/blob/main/app/controllers/api/v2/licenses_controller.rb, https://gumroad.com/pricing.
