# Appendices

The manuscript-facing appendix pages: Glossary, References, and an index pointing to the standalone worksheet/template/checklist files (which live at the top level in `/worksheets`, `/templates`, `/checklists` so they can be used independently of the book).

Populated during Phase 17.
