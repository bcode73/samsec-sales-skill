# Fonts

Licensed typefaces used in manuscript layout and export. Kept here (rather than relying on machine-installed fonts) so DOCX/PDF/EPUB/print builds are reproducible on any machine. Only fonts with a licence permitting embedding/redistribution for this project are stored here; note the licence terms alongside each font file when added.
